.lab-home-active {
  overflow-x: hidden;
}

.lab-experience {
  position: relative;
  width: 100%;
  overflow: clip;
  background: #02040b;
}

.lab-home {
  position: relative;
  height: calc(100dvh - 56px);
  min-height: calc(100dvh - 56px);
  background: #e8e4da;
  color: #283c40;
  overflow: hidden;
}

.lab-home--leave {
  opacity: 0.72;
  transition: opacity 180ms ease;
}

.lab-home--review {
  display: grid;
  grid-template-columns: minmax(200px, 260px) 1fr;
  grid-template-rows: 1fr;
  background: #f4f5f0;
}

.lab-home--review .lab-canvas {
  position: relative !important;
  inset: auto !important;
  width: 100% !important;
  height: 100% !important;
  grid-column: 2;
  grid-row: 1;
}

.lab-review {
  grid-column: 1;
  grid-row: 1;
  display: flex;
  flex-direction: column;
  gap: 0.55rem;
  padding: 0.85rem 1rem 0.85rem 1.1rem;
  overflow: hidden;
  color: #283c40;
  min-height: 0;
}

.lab-review__label {
  margin: 0;
  font-size: 0.72rem;
  letter-spacing: 0.12em;
  text-transform: uppercase;
  font-weight: 700;
  color: #4e827b;
}

.lab-review__image {
  width: 100%;
  flex: 1 1 auto;
  min-height: 0;
  object-fit: contain;
  background: #fff;
  border-radius: 12px;
  border: 1px solid rgba(40, 60, 64, 0.1);
}

.lab-review__views {
  display: flex;
  gap: 0.4rem;
  align-self: start;
}

.lab-review__views button {
  flex: 1;
  border: 1px solid rgba(40, 60, 64, 0.16);
  background: rgba(255, 255, 255, 0.7);
  border-radius: 999px;
  padding: 0.32rem 0.4rem;
  font-size: 0.78rem;
  color: inherit;
}

.lab-review__views button.is-active {
  background: #4e827b;
  color: #f6f1e6;
  border-color: #4e827b;
}

.lab-review__stats {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 0.45rem 0.7rem;
  margin: 0;
}

.lab-review__stats div {
  display: flex;
  justify-content: space-between;
  gap: 0.5rem;
  font-size: 0.78rem;
}

.lab-review__stats dt {
  color: #5b6f72;
}

.lab-review__stats dd {
  margin: 0;
  font-weight: 600;
}

.lab-canvas {
  position: absolute !important;
  inset: 0;
  width: 100% !important;
  height: 100% !important;
  display: block;
  touch-action: none;
}

.lab-home canvas {
  display: block;
  width: 100% !important;
  height: 100% !important;
}

.lab-hud {
  position: absolute;
  inset: 0;
  pointer-events: none;
  z-index: 2;
}

.lab-brand {
  position: absolute;
  left: 1.1rem;
  top: 1rem;
  max-width: 22rem;
}

.lab-brand__mark {
  margin: 0;
  font-size: 0.92rem;
  letter-spacing: 0.08em;
  text-transform: uppercase;
  font-weight: 700;
}

.lab-brand__hint {
  margin: 0.35rem 0 0;
  font-size: 0.92rem;
  line-height: 1.4;
  color: #3d5558;
}

.lab-brand__asset {
  margin: 0.3rem 0 0;
  font-size: 0.78rem;
  line-height: 1.35;
  color: #5b6f72;
}

.lab-directory {
  pointer-events: auto;
  position: absolute;
  right: 1rem;
  top: 1rem;
  width: auto;
  min-width: 8.5rem;
  background: rgba(232, 228, 218, 0.9);
  border: 1px solid rgba(40, 60, 64, 0.12);
  border-radius: 12px;
  padding: 0.28rem;
  backdrop-filter: blur(8px);
}

.lab-directory.is-open {
  width: min(15rem, calc(100vw - 2rem));
  padding: 0.45rem 0.55rem 0.55rem;
}

.lab-directory__toggle {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 0.7rem;
  width: 100%;
  border: 0;
  background: transparent;
  color: inherit;
  padding: 0.28rem 0.45rem;
  border-radius: 8px;
  font: inherit;
}

.lab-directory__toggle span {
  font-size: 0.72rem;
  letter-spacing: 0.12em;
  text-transform: uppercase;
  color: #4e827b;
  font-weight: 700;
}

.lab-directory__toggle small {
  color: #5b6f72;
}

.lab-directory__title {
  margin: 0 0 0.45rem;
  font-size: 0.72rem;
  letter-spacing: 0.12em;
  text-transform: uppercase;
  color: #4e827b;
}

.lab-directory ul {
  list-style: none;
  margin: 0;
  padding: 0;
}

.lab-directory__link {
  display: flex;
  flex-direction: column;
  padding: 0.38rem 0.2rem;
  color: inherit;
  text-decoration: none;
  border-radius: 8px;
}

.lab-directory__link:hover,
.lab-directory__link:focus-visible {
  background: rgba(78, 130, 123, 0.12);
  outline: none;
}

.lab-directory__link span {
  font-weight: 600;
}

.lab-directory__link small {
  color: #5b6f72;
}

.lab-tooltip {
  pointer-events: none;
  position: absolute;
  left: 1.1rem;
  bottom: 1.2rem;
  max-width: 20rem;
  background: rgba(40, 60, 64, 0.9);
  color: #f6f1e6;
  border-radius: 12px;
  padding: 0.7rem 0.85rem;
}

.lab-tooltip span {
  display: block;
  font-size: 0.75rem;
  opacity: 0.75;
  margin-bottom: 0.25rem;
}

.lab-tooltip p {
  margin: 0.2rem 0 0;
  font-size: 0.88rem;
}

.lab-simple {
  pointer-events: auto;
  position: absolute;
  right: 1rem;
  bottom: 1.1rem;
  border: 1px solid rgba(40, 60, 64, 0.18);
  background: rgba(232, 228, 218, 0.92);
  color: inherit;
  border-radius: 999px;
  padding: 0.4rem 0.8rem;
  font-size: 0.82rem;
}

.lab-loading {
  position: absolute;
  inset: 0;
  display: grid;
  place-items: center;
  background: #e8e4da;
  z-index: 3;
  font-size: 0.95rem;
  letter-spacing: 0.04em;
}

.lab-fallback {
  position: absolute;
  inset: 0;
  display: flex;
  align-items: flex-end;
  overflow: hidden;
  background: #d8ccba;
}

.lab-fallback__image {
  position: absolute;
  inset: 0;
  width: 100%;
  height: 100%;
  object-fit: cover;
  object-position: 50% 50%;
  filter: saturate(0.82) contrast(0.96);
}

.lab-fallback__shade {
  position: absolute;
  inset: 0;
  background:
    linear-gradient(
      90deg,
      rgba(22, 35, 34, 0.84),
      rgba(22, 35, 34, 0.16) 58%,
      rgba(22, 35, 34, 0.04)
    ),
    linear-gradient(0deg, rgba(15, 25, 25, 0.5), transparent 60%);
}

.lab-fallback__copy {
  position: relative;
  z-index: 1;
  width: min(44rem, calc(100% - 3rem));
  margin: clamp(1.5rem, 5vw, 5rem);
  padding: clamp(1.2rem, 2.4vw, 2rem);
  text-align: left;
  color: #f4f0e7;
  background: rgba(28, 45, 43, 0.78);
  border: 1px solid rgba(255, 255, 255, 0.2);
  border-radius: 1.2rem;
  box-shadow: 0 28px 80px rgba(12, 23, 22, 0.24);
  backdrop-filter: blur(14px);
}

.lab-fallback__eyebrow {
  color: #b6d0c0;
  font-size: 0.68rem;
  font-weight: 700;
  letter-spacing: 0.18em;
}

.lab-fallback__copy h2 {
  margin: 0.45rem 0 0;
  font:
    400 clamp(2rem, 4vw, 3.8rem) / 1 Georgia,
    serif;
}

.lab-fallback__copy p {
  margin: 0.7rem 0 1.2rem;
  color: rgba(244, 240, 231, 0.72);
}

.lab-fallback__copy ul {
  display: flex;
  flex-wrap: wrap;
  gap: 0.55rem;
  margin: 0;
  padding: 0;
  list-style: none;
}

.lab-fallback__copy a {
  display: grid;
  gap: 0.15rem;
  min-width: 8.5rem;
  padding: 0.72rem 0.9rem;
  color: #f4f0e7;
  text-decoration: none;
  background: rgba(255, 255, 255, 0.06);
  border: 1px solid rgba(255, 255, 255, 0.19);
  border-radius: 0.75rem;
  transition:
    background 0.2s ease,
    transform 0.2s ease;
}

.lab-fallback__copy a:hover {
  background: rgba(255, 255, 255, 0.14);
  transform: translateY(-2px);
}
.lab-fallback__copy a span {
  font-weight: 650;
}
.lab-fallback__copy a small {
  color: rgba(244, 240, 231, 0.58);
}

@media (max-width: 768px) {
  .lab-directory {
    top: auto;
    bottom: 3.6rem;
    right: 0.7rem;
    left: auto;
    width: auto;
  }

  .lab-directory.is-open {
    left: 0.7rem;
    right: 0.7rem;
    width: auto;
  }

  .lab-brand {
    left: 0.8rem;
    right: 0.8rem;
    max-width: none;
  }

  .lab-tooltip {
    left: 0.8rem;
    right: 0.8rem;
    max-width: none;
    bottom: 11.5rem;
  }
}

/* The camera remains visible behind a compact native, keyboard-accessible dialog. */
.lab-inspect-picker {
  position: absolute;
  bottom: 20px;
  left: 20px;
  pointer-events: auto;
  display: grid;
  gap: 5px;
  font-size: 12px;
}
.lab-inspect-picker select {
  max-width: 240px;
  padding: 10px 14px;
  border: 1px solid #ffffff90;
  border-radius: 14px;
  background: #f2eee6e6;
  color: #314840;
}
.lab-detail {
  pointer-events: auto;
  max-height: calc(100dvh - 40px);
  overflow-y: auto;
  position: fixed;
  inset: auto 28px 30px auto;
  width: min(360px, calc(100vw - 32px));
  margin: 0;
  padding: 25px;
  border: 1px solid #ffffffba;
  border-radius: 24px;
  background: rgba(245, 247, 239, 0.78);
  color: #263d36;
  backdrop-filter: blur(24px) saturate(125%);
  box-shadow:
    0 18px 70px #1429222b,
    inset 0 1px #fff;
}
.lab-detail::backdrop {
  background: rgba(24, 35, 29, 0.035);
}
.lab-detail h2 {
  font-size: 23px;
  margin: 10px 0;
  padding-right: 15px;
}
.lab-detail p {
  font-size: 14px;
  line-height: 1.65;
}
.lab-detail .lab-detail__citation {
  margin: 0.8rem 0 0;
  padding-top: 0.75rem;
  border-top: 1px solid rgba(38, 61, 54, 0.12);
  color: #527069;
  font-size: 12px;
}
.lab-detail .lab-detail__eyebrow {
  font-size: 11px;
  text-transform: uppercase;
  letter-spacing: 0.14em;
  margin: 0;
}
.lab-detail__close {
  position: absolute;
  right: 12px;
  top: 10px;
  border: 0;
  border-radius: 50%;
  background: #ffffff90;
  width: 36px;
  height: 36px;
  padding: 0;
  display: grid;
  place-items: center;
  line-height: 1;
  cursor: pointer;
  color: inherit;
}
.lab-detail__actions {
  display: flex;
  flex-wrap: wrap;
  gap: 8px;
  margin-top: 20px;
}
.lab-detail__actions button,
.lab-detail__actions a {
  border: 1px solid #47685722;
  border-radius: 12px;
  padding: 10px 13px;
  background: #ffffffaa;
  color: inherit;
  font-size: 12px;
  font-family: inherit;
  text-decoration: none;
  cursor: pointer;
}
.lab-detail button:focus-visible,
.lab-detail a:focus-visible,
.lab-inspect-picker select:focus-visible {
  outline: 3px solid #48897c;
  outline-offset: 3px;
}
@media (max-width: 600px) {
  .lab-detail {
    bottom: 16px;
    right: 16px;
    padding: 20px;
  }
  .lab-inspect-picker {
    left: 12px;
    bottom: 65px;
  }
  .lab-inspect-picker select {
    max-width: 195px;
  }
}

.lab-home-active footer {
  display: none;
}
